version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      # build the test image
      - run: docker build -t screen-reader-adventures-test -f Dockerfile.backstopjstest .
      # run the tests
      - run: docker run --rm screen-reader-adventures-test

